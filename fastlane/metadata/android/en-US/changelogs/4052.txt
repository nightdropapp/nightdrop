No user-visible changes in this one.

The Android build now states explicitly that it shrinks and minifies its code, rather than relying on a default that could change without us noticing. Both were already happening, so the app itself behaves exactly as 0.1.18 did — this only makes the build say so, at the request of the F-Droid reviewer.
