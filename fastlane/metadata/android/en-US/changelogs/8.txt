Reproducible builds: this release is built exactly the way F-Droid builds it, so anyone can rebuild the APK from source and get an identical result. Two build bugs were fixed to make that true — the bundled Rust core was compiled with whatever Rust "stable" was that day instead of the pinned version, and the flags that make builds machine-independent were being ignored. No functional changes from 0.1.6.
