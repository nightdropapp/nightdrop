Privacy: trimmed the Android permission set. Removed permissions that bundled plugins pulled in but Night Drop never uses — audio recording, external-storage read/write, media (image/video/audio) access, and boot-completed. File attachments and backups still work (they use the Storage Access Framework, which needs no broad storage permission). No other functional changes from 0.1.5.
